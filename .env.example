# Cloud SQL MySQL (or local MySQL for development)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=tsy
DB_PASSWORD=changeme
DB_NAME=treasury

# API
PORT=3000
HOST=0.0.0.0

# Ingest
# Number of calendar days to re-upsert on daily sync (default 7)
SYNC_DAYS=7
